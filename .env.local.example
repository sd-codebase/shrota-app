# Database
DB_USER=shrota
DB_PASSWORD=shrota123
DB_NAME=shrota

# Backend
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
JWT_SECRET_KEY=local-dev-secret-key

# Notification settings (development = console only, production = send emails)
NOTIFICATION_MODE=development

# SMTP Configuration (only needed if NOTIFICATION_MODE=production)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@shrota.in
SMTP_FROM_NAME=Shrota Audiobooks
SMTP_USE_TLS=true

# Frontend
VITE_API_URL=http://localhost:8000
VITE_CDN_URL=http://localhost:8080
